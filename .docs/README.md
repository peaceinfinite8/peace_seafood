Documentation (consolidated)

This repository's documentation has been consolidated under the `.docs` directory.

Top-level items:

- `pages_map.json` — site map for documentation navigation
- `implementation_Plan/` — implementation plans and checklists
- `RELEASE_NOTES/` — release notes
- `resources/` — UI resources (e.g., `resources/ui/palette.md`)

Quick links:

- See the overview: `.docs/00_overview.md` (if present)
- See the master changelog: `.docs/changes/00-master-changelog.md`

If you want a generated HTML index or a different organization (Index file, sidebar, or mkdocs config), tell me and I will create it.
