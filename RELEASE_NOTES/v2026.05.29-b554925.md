# Release snapshot v2026.05.29-b554925

**Tag:** `v2026.05.29-b554925`  
**Commit:** `b554925`  
**Branch:** `main` (force-updated)

## Ringkasan singkat
Force-push dilakukan untuk menyelaraskan versi lokal sebagai snapshot rilis. Simpan backup tag: `backup-main-b554925`.

## Commit utama (terbaru)
- b554925 feat: sync local changes
- fd415b3 feat: add branded 403/404 error pages with dark mode support
- 2500e3f security: add server-side role guards for sensitive pages and API endpoints
- 437bcde fix: include activity_log in reset script truncate list
- af884b3 fix: activity log UI, stock validation, penitipan bug & autotest runner
- 498fa4b chore: improve Activity Log UX; timezone DB session set to +07:00; add kg formatting & validation

## Perubahan penting / catatan
- Beberapa file dan struktur folder diubah secara besar-besaran. Tim yang bekerja dari clone lama harus sinkron ulang (lihat langkah di bawah).
- Force-push menimpa riwayat remote. Pastikan semua kontributor menyimpan pekerjaan local sebelum mengikuti reset.

## Tindakan yang disarankan
1. Kontributor: simpan kerja lokal (stashes/branch baru) lalu sinkron:

```bash
git fetch origin
git reset --hard origin/main
```

2. Jalankan pemeriksaan lokal (disarankan):
- Pasang PHP dan jalankan `php -l` pada file PHP
- `composer install` dan jalankan test suite (jika ada)

3. CI/Release: buat draft GitHub Release menggunakan tag `v2026.05.29-b554925` dan gunakan catatan ini sebagai isi rilis.

## Catatan keamanan
- Jika repo publik, tidak ada kredensial yang dikommit; tetap periksa file konfigurasi seperti `.env.example`.

---
Jika Anda mau, saya bisa langsung membuat draft Release di GitHub (butuh `gh` atau token), atau saya bisa bikin PR/menambahkan changelog lebih detil. Mau saya lanjut buat draft Release di GitHub (Anda perlu login/beri token), atau cukup simpan draf ini di repo?